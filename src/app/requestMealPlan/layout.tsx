import * as React from 'react';

export default function RequestMealPlanLayout({
                                             children
                                         }: {
    children: React.ReactNode;
}) {
    return <>
        {children}
    </>;
}
