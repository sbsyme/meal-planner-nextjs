import * as React from 'react';

export default function MealPlanSummaryLayout({
                                             children
                                         }: {
    children: React.ReactNode;
}) {
    return <>
        {children}
    </>;
}
